import 'package:flutter/material.dart';

class AgregarItemScreen extends StatelessWidget {
  const AgregarItemScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const  Scaffold(
      body: Center(child: Text('Agregar Item Screen')),
    );
  }
}